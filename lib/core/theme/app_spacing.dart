/// MangoPos Design System - Spacing System
/// All spacing values are in density-independent pixels (dp)
/// Based on 4dp grid system
class AppSpacing {
  // ============================================================================
  // BASE SPACING (múltiplos de 4dp)
  // ============================================================================

  static const double xs = 4.0;
  static const double sm = 8.0;
  static const double md = 12.0;
  static const double lg = 16.0;
  static const double xl = 20.0;
  static const double xxl = 24.0;
  static const double xxxl = 32.0;

  // ============================================================================
  // SPACING ESPECÍFICO DEL DASHBOARD
  // ============================================================================

  /// Padding del contenedor principal - 24dp (p-6)
  static const double containerPadding = 24.0;

  /// Gap entre secciones - 24dp (gap-6)
  static const double sectionGap = 24.0;

  /// Padding de cards - 24dp (p-6)
  static const double cardPadding = 24.0;

  /// Gap entre items - 16dp (gap-4)
  static const double itemGap = 16.0;

  /// Gap apretado - 12dp (gap-3)
  static const double tightGap = 12.0;
}
